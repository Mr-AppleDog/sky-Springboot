<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        insert into sky_take_out.setmeal_dish (setmeal_id, dish_id, name,
        copies,
        price)
        values
        <foreach collection="setmealDishes" item="item" separator=",">
            (#{item.setmealId},#{item.dishId},#{item.name},#{item.copies},#{item.price})
        </foreach>
    </insert>


    <select id="getById" resultType="com.sky.entity.SetmealDish">
        select * from sky_take_out.setmeal_dish where dish_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>

    </select>
</mapper>
